services:
    recipes:
        build: .
        container_name: recipes
        ports:
            - "8081:8080"
        restart: unless-stopped
        volumes:
            - ./data:/app/data
        env_file:
            - .env
