services:
    dagensbilde:
        build: .
        container_name: dagensbilde
        ports:
            - "8080:8080"
        restart: unless-stopped
        env_file:
            - .env
        environment:
            - DB_PATH=/data/app.db
            - IMAGE_DIR=/data/images
        volumes:
            - ./data:/data
